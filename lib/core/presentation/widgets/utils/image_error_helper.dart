import 'package:flutter/material.dart';

abstract final class ImageErrorHelper {
  static Widget errorIcon(
    BuildContext _,
    String __,
    // ignore: avoid_annotating_with_dynamic
    dynamic ___,
  ) =>
      const Icon(Icons.error);
}
